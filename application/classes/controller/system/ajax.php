<?php defined('SYSPATH') or die('No direct script access.');/* * Главный ajax controller */class Controller_System_Ajax extends Controller_System_Security{	/**	 * Массив данных ответа	 */	protected $data = array(		'success'  => FALSE,   // Успешное или неуспешное выполнение		'msg_text' => '',      // Текст сообщения		'msg_type' => '',      // Тип сообщения		'params'   => array(), // Дополнительные передаваемые данные	);	/**	 * Before action	 */	public function before()	{		parent::before();		// Если запрос не аяксовый		if ( ! Request::current()->is_ajax())		{			// Error 404			//throw new HTTP_Exception_404();		}	}}